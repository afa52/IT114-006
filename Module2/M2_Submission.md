<table><tr><td> <em>Assignment: </em> IT114 M2 Java-HW</td></tr>
<tr><td> <em>Student: </em> Anthony Aliotta (afa52)</td></tr>
<tr><td> <em>Generated: </em> 2/5/2023 8:18:28 PM</td></tr>
<tr><td> <em>Grading Link: </em> <a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT114-006-S23/it114-m2-java-hw/grade/afa52" target="_blank">Grading</a></td></tr></table>
<table><tr><td> <em>Instructions: </em> <p><br></p><p><strong>Template Files</strong>&nbsp;You can find all 3 template files in this gist:&nbsp;<a href="https://gist.github.com/MattToegel/fdd2b37fa79a06ace9dd259ac82728b6">https://gist.github.com/MattToegel/fdd2b37fa79a06ace9dd259ac82728b6</a>&nbsp;<br></p><p>Setup steps:</p><ol><li><code>git checkout main</code></li><li><code>git pull origin main</code></li><li><code>git checkout -b M2-Java-HW</code></li></ol><p>You'll have 3 problems to save for this assignment.</p><p>Each problem you're given a template&nbsp;<strong>Do not edit anything in the template except where the comments tell you to</strong>.</p><p>The templates are done in such a way to make it easier to capture the output in a screenshot.</p><p>You'll copy each template into their own separate .java files, immediately git add, git commit these files (you can do it together) so we can capture the difference/changes between the templates and your additions. This part is required for full credit.</p><p>HW steps:</p><ol><li>Open VS Code at the root of your repository folder</li><li>In VS Code create a new folder/directory called M2</li><li>Create 3 new files in this new M2 folder (Problem1.java, Problem2.java, Problem3.java)</li><li>Paste each template into their respective files</li><li><code>git add .</code></li><li><code>git commit -m "adding template baselines</code></li><li>Do the related work (you may do steps 8 and 9 as often as needed or you can do it all at once at the end)</li><li><code>git add .</code></li><li><code>git commit -m "completed hw"</code></li><li>When you're done push the branch<ol><li><code>git push origin M2-Java-HW</code></li></ol></li><li>Create the Pull Request with <b>main</b>&nbsp;as base and&nbsp;<strong>M2-Java-HW</strong>&nbsp;as compare (don't merge/close it yet)</li><li>Create a new file in the M2 folder in VS Code called m2_submission.md</li><li>Fill out the below deliverable items, save the submission, and copy to markdown</li><li>Paste the markdown into the m2_submission.md</li><li>add/commit/push the md file<ol><li><code>git add m2_submission.md</code></li><li><code>git commit -m "adding submission file"</code></li><li><code>git push origin M2-Java-HW</code></li></ol></li><li>Merge the pull request from step 11</li><li>On your local machine sync the changes<ol><li><code>git checkout main</code></li><li><code>git pull origin main</code></li></ol></li><li>Submit the link to the m2_submission.md file from the main branch to Canvas</li></ol><p><br></p></td></tr></table>
<table><tr><td> <em>Deliverable 1: </em> Problem 1 - Only output Odd values of the Array under "Odds output" </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 1 showing the data and show the code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216855951-9f7d3c57-e088-459f-9849-0127568c53b2.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the edited code required to complete the given directions.<br>The screenshot also contains commented code that describes what each line of code<br>is doing.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216856003-54521183-b931-46be-81c8-3d272a0beaf5.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the output of the Problem1 with implemented code.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>The code implemented is a for loop that iterates thru the array and<br>checks if the current element is odd by using a division remainder method.<br>Any number divided by 2 that returns a remainder of 1 is an<br>odd number. That is why I divided the current element in the array<br>(arr[i]) by 2 and if the element is equals (==) to 1, then<br>the system would output that current element in the array.<br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 2: </em> Problem 2 - Only output the sum/total of the array values (the number must end in 2 decimal places, if it ends in 1 it must have a 0 at the end) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 2 showing the data and show the code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216856537-5ed8f0d3-1a9c-44e2-8d74-ab9850be51e2.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the edited code required to complete the given directions.<br>The screenshot also contains commented code that describes what each line of code<br>is doing.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216856539-33eb91c7-2646-4c49-a504-ebdfa89675ac.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the output of the Problem2 with implemented code.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>The totaling logic was completed by implementing a for loop that iterates thru<br>the array and for each element the system adds the value of the<br>current element to the variable &quot;Total&quot;.&nbsp;<div>The rounding logic was completed by assigning the<br>String variable &quot;totalOutput&quot; to a formatted string that takes in parameters &quot;% .2f&quot;<br>which tells Java to return the variable with 2 decimal places starting from<br>the format specifier and &quot;total&quot; which is the double value from the array<br>and it is converted to a String.</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 3: </em> Problem 3 - Output the given values as positive under the "Positive Output" message (the data otherwise shouldn't change) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> 2 Screenshots: Clearly screenshot the output of Problem 3 showing the data and show the code</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216857393-7ae4cbd4-96a3-4c22-b556-d0aec775819f.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the edited code required to complete the given directions.<br>The screenshot also contains commented code that describes what each line of code<br>is doing.<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/123400241/216857418-03ebfe79-5b49-4feb-92f7-26ad058defed.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>The following screenshot is the output of the Problem3 with implemented code.<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>Positive conversion was done by first iterating through the array and checking if<br>the current object/element in the array is an instance of the data type<br>listed by using the instanceof keyword. Since all array elements are objects, I<br>considered using this method to only handle arr array and not a1, a2,<br>a3, and a4 directly. Then, to convert the instance datatype to a positive<br>number, I used Java&#39;s built in Math class and implemented Math.abs to convert<br>each element to its absolute value.<div>The data was casted by using a wrapper<br>class, which is a way to use primitive data types as objects. For<br>each condition that followed true, the wrapper class wraps the primitive value into<br>an object and that object of whatever primitive it is contains a single<br>field whose type is that primitive data type. Then that casted object is<br>assigned to an index in the output array&nbsp;</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 4: </em> Misc Items </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="https://user-images.githubusercontent.com/54863474/211707773-e6aef7cb-d5b2-4053-bbb1-b09fc609041e.png"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Pull Request URL for M2-Java-HW to main</td></tr>
<tr><td> <a rel="noreferrer noopener" target="_blank" href="https://github.com/afa52/IT114-006/pull/4">https://github.com/afa52/IT114-006/pull/4</a> </td></tr>
<tr><td> <em>Sub-Task 2: </em> Talk about what you learned, any issues you had, how you resolve them</td></tr>
<tr><td> <em>Response:</em> <p>The first two problems were generally easy but I had trouble figuring out<br>how to do the third problem. I was uncertain at first on how<br>to handle the object arr and not the arrays individually. After doing some<br>research, I discovered that the instanceof keyword really helps because it tested if<br>the current object (in arr) is an instance of a specific class which<br>I defined in three different conditions.<br></p><br></td></tr>
</table></td></tr>
<table><tr><td><em>Grading Link: </em><a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IT114-006-S23/it114-m2-java-hw/grade/afa52" target="_blank">Grading</a></td></tr></table>